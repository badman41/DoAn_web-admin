import React from 'react'
import { Checkbox } from 'antd'

const SolverConfig = props => (
  <div>
    <h2>Cấu hình chạy</h2>
    <Checkbox>Cân bằng tải</Checkbox>
  </div>
)

SolverConfig.propTypes = {}

export default SolverConfig
